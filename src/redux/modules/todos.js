const initalState = {
  id: 0,
  title: '',
  text: '',
  isDone: false,
}

const todos = (state = initalState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default todos
